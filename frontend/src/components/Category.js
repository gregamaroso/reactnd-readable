import React, { Component } from 'react';

export default class Category extends Component {
  render() {
    return (
      <div>
        Show posts in a single category here.<br />
        Under each post will be the threaded comments.
      </div>
    )
  }
}
