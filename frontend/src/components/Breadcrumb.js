import React, { Component } from 'react';

export default class Breadcrumb extends Component {
  render() {
    const { breadcrumb } = this.props;

    return (
      <div className="breadcrumb">blah</div>
    );
  }
};
